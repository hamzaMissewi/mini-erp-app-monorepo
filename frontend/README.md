# Frontend — Next.js App

This folder contains the Next.js 15 (App Router) frontend for the Mini ERP project.

Quick start (from repo root):

```bash
pnpm install
pnpm --filter @app/frontend dev
```

Configure environment variables by copying `.env.example`.
